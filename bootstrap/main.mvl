include "../std/io.mvl"

proc main argc: s64 argv: s64
  if argc <= 1
    @println "ERROR: input file was not provided"
     @exit 1
  end

  ptr = !add argv 8
  input_path = *s64 ptr

  @print "Input file path: "
  @println input_path
end
