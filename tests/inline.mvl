proc main() -> s64:
  #result = add(2, 2)
  result = inline_add(2, 2)
  #inline_add(2, 2)

  retval result
end

proc add(a: s64, b: s64) -> s64:
  c = a
  asm "add @r,@a" [c, b]
  retval c
end

proc inline inline_add(a: s64, b: s64) -> s64:
  c = a
  asm "add @r,@a" [c, b]
  retval c
end
